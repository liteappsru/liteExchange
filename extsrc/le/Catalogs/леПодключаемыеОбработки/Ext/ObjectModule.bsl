
Процедура ПриЗаписи(Отказ)
	Данные = Хранилище.Получить();
	Если Не Данные = Неопределено Тогда
		ИмяФайла = леСерверПовтИсп.КаталогОбработчиков() + Наименование + ".erf";
		Данные.Записать(ИмяФайла);
	КонецЕсли;
КонецПроцедуры
