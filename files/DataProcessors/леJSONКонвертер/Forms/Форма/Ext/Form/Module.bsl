
&НаСервере
Процедура ТестПроизводительностиНаСервере()
	обО = РеквизитФормыВЗначение("Объект");
	Старт = ТекущаяУниверсальнаяДатаВМиллисекундах();
	Для Сч = 1 по 1000 Цикл
		обО.ДатаИзJSON("20200101T235959");
	КонецЦикла;
	Финиш = ТекущаяУниверсальнаяДатаВМиллисекундах();
КонецПроцедуры

&НаКлиенте
Процедура ТестПроизводительности(Команда)
	ТестПроизводительностиНаСервере();
КонецПроцедуры
